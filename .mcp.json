{
  "mcpServers": {
    "printful": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/crichalchemist/printful-mcp@main",
        "printful-mcp"
      ],
      "env": {
        "PRINTFUL_API_KEY": "${PRINTFUL_API_KEY}"
      }
    }
  }
}
